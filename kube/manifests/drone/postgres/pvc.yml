apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-drone-pvc
spec:
  accessModes:
    #- ReadWriteOnce
    - ReadWriteMany
  storageClassName: freenas-nfs-csi
  resources:
    requests:
      storage: 15Gi
