apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  namespace: gifinator
spec:
  rules:
  - host: gif.kates.test
    http:
      paths:
      - path: /
        backend:
          serviceName: frontend-service
          servicePort: 8090
