---

- name: get ready for sentry
  hosts:
    - sentry
  become: true

  tasks:
    - name: install git
      apt:
        name:
          - git
  vars:
    pip_install_packages:
      - name: docker
    docker_users:
      - "{{ ansible_user }}"

  roles:
    - geerlingguy.pip
    - geerlingguy.docker

...
