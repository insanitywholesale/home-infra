---

- name: install docker
  hosts:
    - swarm
  become: true

  tasks:
  vars:
    pip_install_packages:
      - name: docker
    docker_users:
      - "{{ ansible_user }}"

  roles:
    - geerlingguy.pip
    - geerlingguy.docker

...
